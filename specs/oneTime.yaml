fetch_oneTime:
type: object
description: one time fetching of data
required:
  - requestedDataTypes
  - dateRange
properties:
  requestedDataTypes:
    type: array
    description: Schema locations for the requested data type.
    items:
      type: url
  dateRange:
    type: object
    required:
      - from
      - to
    properties:
      from:
        type: string
        format: date-time
      to:
        type: string
        format: date-time

