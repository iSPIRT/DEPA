fetch_subscription:
type: object
properties:
  requestedEventTypes:
    type: array
    description: Schema locations for the requested data type.
    items:
      type: url
  dateRange:
    type: object
    description: subscription events will be delivered between given date range.
    required:
      - from
      - to
    properties:
      from:
        type: string
        format: date-time
      to:
        type: string
        format: date-time
